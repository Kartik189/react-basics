import React from "react";

const Header = (props) => {
        return(
            <nav>
                <div>
                    <a href="#">Home</a>
                </div>
            </nav>
        )
}

export default Header;